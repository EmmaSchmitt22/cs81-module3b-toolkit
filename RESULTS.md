The double() function takes a number and doubles its value.

The square() function provides the square value of a number.

The isEven() function confirms if a number is even.

The isOdd() function confirms if a number is odd.

The multiply() function accepts two values and multiplies them against each other.
